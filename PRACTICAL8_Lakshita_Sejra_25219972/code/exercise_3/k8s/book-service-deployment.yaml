apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -o k8s/
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: book-service
  name: book-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: book-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o k8s/
        kompose.version: 1.37.0 (HEAD)
      labels:
        io.kompose.service: book-service
    spec:
      containers:
        - env:
            - name: POSTGRES_DB
              value: library_management_db
            - name: POSTGRES_HOST
              value: library-management-db
            - name: POSTGRES_PASSWORD
              value: password
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_USER
              value: user
          image: lakshitasejra/library-book-service:exercise3
          name: book-service
          ports:
            - containerPort: 5006
              protocol: TCP
      restartPolicy: Always
